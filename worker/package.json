{
  "name": "herd-and-seek-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:e2e": "wrangler dev --port 8787 --var INTERNAL_REWARD_SECRET:local-e2e-only --var ADMIN_KEY:local-e2e-admin",
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260619.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}
